[project]
name = "plan-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "anaplan-sdk[cert,oauth]>=0.5.13",
    "fastmcp[apps]>=3.2.4",
    "keyring>=25.7.0",
    "pytest>=9.0.3",
]
