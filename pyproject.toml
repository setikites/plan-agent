[project]
name = "plan-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "anaplan-sdk[cert,oauth]>=0.5.13",
    "fastmcp>=3.2.4",
    "pytest>=9.0.3",
]
